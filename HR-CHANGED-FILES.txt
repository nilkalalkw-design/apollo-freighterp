HR PORTAL V2 - FIXES

FIXED
- HR Admin Leave Approvals now always uses the dedicated HR module; no fallback to the old legacy panel after the module loads.
- HR Calendar & Rules now always uses the dedicated HR calendar panel.
- HR Leave Balances and HR Leave Policies use the dedicated HR module.
- HR refresh now re-renders all HR admin panels, not only My Leave/Leave Approvals.
- HR Admin employee documents are scoped to the selected employee on both list and upload APIs.
- HR Admin document viewing is authorized independently of the admin's own employee documents.
- Regular employees remain restricted to their own documents.

FILES CHANGED
- web/app-runtime.js
- web/hr-portal.js
- server/src/index.js

DATABASE
- No new migration required for these fixes. Existing 021_hr_leave_management.sql remains.
