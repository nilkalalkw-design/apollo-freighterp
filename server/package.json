{
  "name": "apollofreighterp-server",
  "version": "1.0.0",
  "private": true,
  "description": "Separate Render-ready API for ApolloFreightERP",
  "main": "src/index.js",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "check": "node --check src/index.js && node --check src/migrate.js",
    "migrate": "node src/migrate.js",
    "db:setup": "node src/migrate.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "helmet": "^8.0.0",
    "morgan": "^1.10.0",
    "pg": "^8.13.1"
  }
}
